packages:
  - packages/*

# Skip re-verifying/installing deps before each `pnpm run` script.
verifyDepsBeforeRun: false

# Keep security-sensitive transitive build dependencies on patched releases.
overrides:
  vite: ">=6.4.3"
  esbuild: ">=0.28.1"

onlyBuiltDependencies:
  - esbuild
